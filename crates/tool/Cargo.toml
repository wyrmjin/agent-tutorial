[package]
name = "tool"
version.workspace = true
edition.workspace = true

[dependencies]
provider = { path = "../provider" }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["process"] }
logger = { path = "../logger" }
