[package]
name = "agent"
version.workspace = true
edition.workspace = true

[dependencies]
provider = { path = "../provider" }
tool = { path = "../tool" }
anyhow = "1"
thiserror = "2"
serde_json = "1"
logger = { path = "../logger" }
