[package]
name = "agent"
version.workspace = true
edition.workspace = true

[dependencies]
ai = { path = "../ai" }
tool = { path = "../tool" }
serde_json = "1"
futures = "0.3"
tokio = { version = "1", features = ["sync", "macros", "rt"] }
tracing = "0.1"

[dev-dependencies]
async-trait = "0.1"
