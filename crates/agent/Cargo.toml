[package]
name = "agent"
version.workspace = true
edition.workspace = true

[dependencies]
ai = { path = "../ai" }
tool = { path = "../tool" }
thiserror = "2"
serde_json = "1"
logger = { path = "../logger" }
futures = "0.3"
tokio = { version = "1", features = ["sync", "macros", "rt"] }

[dev-dependencies]
async-trait = "0.1"
